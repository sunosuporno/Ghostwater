# Privy (required for login & embedded wallet)
# 1. Create an app at https://dashboard.privy.io
# 2. Create an "App client" (mobile) and copy App ID + Client ID
# 3. Enable Google OAuth in Dashboard → Login methods → Socials
# 4. Set allowed URL schemes for your app (e.g. ghostwater://) in App clients
EXPO_PUBLIC_PRIVY_APP_ID=your-privy-app-id
EXPO_PUBLIC_PRIVY_CLIENT_ID=your-privy-client-id

# RPC for Base mainnet (used by forge --rpc-url base). Get from Alchemy/Infura/Public.
BASE_RPC_URL=https://mainnet.base.org

# Your L2 Registry address from Durin (after deploying a registry at durin.dev for your ENS name).
# Base mainnet registry address – replace with your deployed registry.
GHOSTWATER_L2_REGISTRY_ADDRESS=0x0000000000000000000000000000000000000000

# Deployer EOA private key (same wallet you used to deploy the L2 Registry). Used only for --broadcast.
# Never commit .env. Use a key with minimal funds for deploys.
PRIVATE_KEY=0x...

# Basescan API key (optional). If set, deploy script will verify the contract at https://basescan.org/myapikey
BASESCAN_API_KEY=

# Deploy (from contracts/):
#   forge script script/Deploy.s.sol --rpc-url base --broadcast
# (Foundry loads .env from this directory and uses PRIVATE_KEY for broadcast.)
# Then on Basescan: Registry.addRegistrar(<GhostwaterRegistrar address>)

# For Base mainnet fork tests (test/GhostwaterRegistrarFork.t.sol):
#   GHOSTWATER_REGISTRAR_ADDRESS=0x...  # Your deployed GhostwaterRegistrar on Base
# Run: cd contracts && forge test --match-contract GhostwaterRegistrarFork -vvv
# Or with URL in command: forge test --match-contract GhostwaterRegistrarFork --fork-url "https://mainnet.base.org" -vvv
import { PoolList } from "@/components/PoolList";

/** Sui: Margin tab. Same list as Deepbook on Base — tap tab → list, tap item → pool. */
export default function TradingListScreen() {
  return <PoolList title="Pools" />;
}
